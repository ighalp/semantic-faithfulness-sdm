# Data Directory

This directory contains example datasets and cached results for the SDM package.

## Directory Structure

```
data/
├── examples/           # Example QCA triplets
│   └── nvidia_10k_sample.json
├── cache/             # Cached embeddings and distributions
│   ├── embeddings/
│   └── distributions/
└── results/           # Analysis results
    └── faithfulness_scores.json
```

## Example Data Format

### QCA Triplet JSON Format

```json
{
  "triplets": [
    {
      "id": "example_1",
      "question": "What are the main risks?",
      "context": "The company faces multiple risks...",
      "answer": "The main risks include...",
      "metadata": {
        "source": "10-K filing",
        "date": "2024-01-01"
      }
    }
  ]
}
```

### Results Format

```json
{
  "analysis_date": "2025-01-20",
  "model_config": {
    "embedding_model": "all-MiniLM-L6-v2",
    "clustering": "udib",
    "num_topics": 23
  },
  "results": [
    {
      "triplet_id": "example_1",
      "F_S": 0.857,
      "SEP_total": 0.167,
      "SEP_system": 0.523,
      "D_min": 0.167,
      "H_Q": 2.341,
      "H_C": 3.128,
      "H_A": 3.651,
      "iterations": 18,
      "converged": true
    }
  ]
}
```

## Caching

The package automatically caches:
1. **Sentence embeddings** → `cache/embeddings/`
2. **Topic distributions** → `cache/distributions/`

This significantly speeds up re-analysis of the same data.

Cache files are named using content hashes to ensure consistency.

## Example Datasets

### NVIDIA 10-K Example

A sample from the experimental dataset used in the paper:
- **Source**: NVIDIA Corporation FY2024 Form 10-K, Item 1A (Risk Factors)
- **Questions**: 10 paraphrased questions (5 comprehensive, 5 focused)
- **Context**: ~60,000 characters of risk disclosures
- **Answers**: Generated by Claude Sonnet 4.5

Located in: `examples/nvidia_10k_sample.json`

## Creating Your Own Dataset

1. **Prepare QCA triplets** in the JSON format above
2. **Run analysis**:
   ```python
   from sdm_package import SemanticMutualInformationAnalyzer
   import json

   with open('data/examples/my_dataset.json', 'r') as f:
       data = json.load(f)

   analyzer = SemanticMutualInformationAnalyzer()
   # ... analysis code ...
   ```

3. **Results are automatically cached** for future use

## Data Privacy

**Important**: If your data contains sensitive information:
- Add `data/` to `.gitignore` (already done)
- Use environment variables for API keys
- Never commit actual data to public repositories
- Consider anonymizing or synthesizing examples

## Citation

If you use the NVIDIA 10-K example dataset, please cite our paper:

```bibtex
@article{halperin2025faithfulness,
  title={Information-Theoretic Faithfulness Metrics for Large Language Models},
  author={Halperin, Igor},
  year={2025}
}
```
